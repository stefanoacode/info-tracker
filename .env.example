# Optional — leave blank to use defaults
NITTER_INSTANCE=  # Leave blank to try public instances, or set e.g. http://localhost:8080

# RSS/Substack, Reddit, and X (via Nitter) work without any API keys
# Use /info-tracker config frequency <value> to set digest frequency
