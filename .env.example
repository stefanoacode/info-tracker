# Optional — leave blank to use defaults
YOUTUBE_API_KEY=
NITTER_INSTANCE=  # Leave blank to try public instances, or set your own e.g. http://localhost:8080

# RSS/Substack, Reddit, and X (via Nitter) work without any API keys
DIGEST_FREQUENCY_HOURS=6
